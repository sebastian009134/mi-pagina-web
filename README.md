# Catalogo Motos - Migrado a React (Vite)

Instrucciones rápidas:

1. Instala dependencias:

```bash
npm install
```

2. Ejecuta en modo desarrollo:

```bash
npm run dev
```

Este proyecto usa Vite + React y mantiene tus estilos originales en `src/styles.css`.
